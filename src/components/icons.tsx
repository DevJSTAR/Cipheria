import { MoonIcon, SunIcon, LaptopIcon, UnlockIcon, Loader2Icon, TrashIcon, CheckIcon, XIcon, AlertTriangleIcon, InfoIcon } from "lucide-react";

export const Icons = {
  sun: SunIcon,
  moon: MoonIcon,
  laptop: LaptopIcon,
  unlock: UnlockIcon,
  spinner: Loader2Icon,
  trash: TrashIcon,
  check: CheckIcon,
  x: XIcon,
  alert: AlertTriangleIcon,
  info: InfoIcon,
};